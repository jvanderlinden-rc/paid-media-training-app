Place your internal training media here and reference them in content blocks.

Examples:
- /sample-walkthrough.mp4 (video)
- /sample-audio.mp3 (audio)

Then update the module content blocks to use those URLs.
